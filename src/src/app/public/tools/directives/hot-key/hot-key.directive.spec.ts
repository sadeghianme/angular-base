import { HotKeyDirective } from './hot-key.directive';

describe('HotKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new HotKeyDirective();
    expect(directive).toBeTruthy();
  });
});
